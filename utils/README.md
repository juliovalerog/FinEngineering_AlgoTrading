# Utils folder

No shared utility module is required at this stage.

This folder is reserved for future reusable helper functions if the teaching repository expands (for example, common plotting functions or performance metric helpers).
